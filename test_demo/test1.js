'use strict'

const expect = require('chai').expect

describe('test_demo demo', () => {
    describe('"up"', () => {
        it('should export a function', () => {
            expect(2).to.equal(2);
        });
    });
});