'use strict'

const expect = require('chai').expect

describe('test_demo demo', () => {
    describe('"up"', () => {
        it('should be 2', () => {
            expect(2).to.equal(2);
        });

    it('should be 3', () => {
        expect(3).to.equal(3);
});
    });
});